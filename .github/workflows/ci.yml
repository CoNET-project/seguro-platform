name: CI
on:
    - push
    - pull_request
jobs:
    install:
        name: Install
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/cache@v2
              with:
                key: install-cache
                path: '*'
            - run: yarn install --frozen-lockfile
    test:
        name: Test
        runs-on: ubuntu-latest
        needs: install
        steps:
            - run: yarn test
    build:
        name: Build
        runs-on: ubuntu-latest
        needs: install
        steps:
            - run: yarn build
